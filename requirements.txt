# Зависимости Telegram Gifts NFT Sniper
anthropic>=0.40.0
requests>=2.31.0

# --- Только для ЖИВОЙ торговли (подпись транзакций TON) ---
# Без них бот полностью работоспособен в режиме записи рынка, бэктеста и
# симуляции покупки; preflight просто не пустит в живой режим.
# ВНИМАНИЕ: tonutils.utils переехал в ОТДЕЛЬНЫЙ пакет ton-core — нужны оба.
tonutils>=2.3.0
ton-core>=0.5.0
